import React from "react";
import styles from "./noteitem.module.css";
import { NoteItem as NoteItemType } from "../../types/types";

const NoteItem = ({ title, date, previewText }: NoteItemType) => {
  return <div></div>;
};
export default NoteItem;
